Use the `run_commands.txt` file to run the project (activate the backend venv first).

Security notes:
- Backend security settings are now env-driven with safe production defaults.
- Local development still works out of the box with `DEBUG=true`.
- See `backend/.env.example` for available variables and rate-limit/security controls.
